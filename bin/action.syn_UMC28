#!/bin/bash

# $@: forward all input arguments to VCS
export SYN_OUT=${WORKING_DIR}/work.syn
\rm -rf ${SYN_OUT}
mkdir -p ${SYN_OUT}
cd ${SYN_OUT}
dc_shell-xg-t < ${WORKING_DIR}/rtl-actions/tcl/syn/UMC28.tcl
cd ${WORKING_DIR}